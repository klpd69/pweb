// Popunder component placeholder - scripts removed
// This component is no longer active

const Popunder = () => {
  // This component doesn't render anything visible
  return null;
};

export default Popunder;
