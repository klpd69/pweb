// AntiAdblock component placeholder - scripts removed
// This component is no longer active

const AntiAdblock = () => {
  // This component doesn't render anything visible
  return null;
};

export default AntiAdblock;
